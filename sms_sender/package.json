{
  "name": "gf-microservices",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prestart:dev": "pm2 stop sms_sender 2> /dev/null || true",
    "start:dev": "MODE=development pm2 start ./server.js --name=sms_sender",
    "prestart:preprod": "pm2 stop sms_sender 2> /dev/null || true",
    "start:preprod": "MODE=preprod pm2 start ./server.js --name=sms_sender",
    "prestart:prod": "pm2 stop sms_sender 2> /dev/null || true",
    "start:prod": "MODE=production pm2 start ./server.js --name=sms_sender"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.23.0",
    "moment": "^2.29.1",
    "node-cron": "^3.0.0",
    "sequelize": "^6.7.0"
  }
}